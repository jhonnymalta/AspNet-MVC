@model ListCoursesViewModel

<div class="container"> 
     <div class="justify-content-center">
        <h2 class="mb-5">Últimos cursos concluídos</h2>

        </div>
    <div class="row justify-content-center">
       
         @foreach (var course in Model.coursesList)
    { 
        <partial name="_CardCourse" ></partial>
       
    }
    </div>

   
</div>


